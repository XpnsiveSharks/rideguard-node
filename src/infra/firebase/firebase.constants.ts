/**
 * DI tokens for the Firebase SDK handles exposed by FirebaseModule.
 * Add one token per SDK surface as it is needed (messaging, firestore, ...).
 */
export const FIREBASE_APP = 'FIREBASE_APP';
export const FIREBASE_AUTH = 'FIREBASE_AUTH';
export const FIREBASE_FIRESTORE = 'FIREBASE_FIRESTORE';
